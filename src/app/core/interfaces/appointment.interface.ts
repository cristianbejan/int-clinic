export interface Appointment {
  specialtyId: string;
  serviceId: string;
  doctorId: string;
  clinicId: string;
  // patientID: string
  // date: string
  // timeSlot: string
  // extraMentions: string
}
